# Health Warning Statement — 27 CFR part 16.
#
# The reference text below is reproduced verbatim from 27 CFR 16.21. The Code of
# Federal Regulations is a U.S. Government work in the public domain (17 U.S.C.
# 105). This file is the ONLY place that text may appear — no source file may
# hardcode it (enforced by tests/test_ssot.py).
#
# The statute (27 U.S.C. 215) fixes the wording; TTB cannot change it by
# regulation, so it is stable. The string is pure ASCII (no curly quotes, no
# apostrophes), which is what makes "any non-ASCII glyph is an OCR artifact" a
# defensible normalization rule.

rules:
  - id: health_warning_verbatim
    title: "Government Warning: Exact Wording"
    citation: 27 CFR 16.21
    beverage_classes: [all]
    severity: fail
    description: >-
      The Government Warning must appear word-for-word (27 CFR 16.21), with the
      two heading words in capital letters (16.22(a)(2)). The heading is
      compared case-sensitively; the remainder is compared on its letters and
      digits alone (case-, spacing-, and punctuation-insensitive) — the
      regulation fixes the body's wording, and OCR of the small print drops
      the spaces between correctly printed words. A changed, omitted, or added
      word fails.
    match:
      strategy: verbatim
      field: government_warning
      reference: |-
        GOVERNMENT WARNING: (1) According to the Surgeon General, women should not drink alcoholic beverages during pregnancy because of the risk of birth defects. (2) Consumption of alcoholic beverages impairs your ability to drive a car or operate machinery, and may cause health problems.

  - id: health_warning_bold
    title: "Government Warning: Bold Heading"
    citation: 27 CFR 16.22(a)(2)
    beverage_classes: [all]
    severity: fail
    description: >-
      The words "GOVERNMENT WARNING" must appear in bold type, and the remainder
      of the statement must not. Checked by comparing the heading's stroke width
      against the body text — beside it on the same line, or size-normalized
      against the statement's own lines when the heading prints alone (that
      cross-line measure can clear a heading, never fail one); abstains to
      review when the text is too small to judge from the image.
    match:
      strategy: warning_bold
      field: government_warning
