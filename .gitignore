.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.DS_Store

# Scratch scrapes land in a root fixtures/ and stay out of the repo. The two
# committed corpora live under tests/fixtures/: the synthetic set (labels/,
# regenerate with: uv run python tools/make_test_labels.py) and the 11-record
# real COLA set (cola/ — public federal registry records; provenance and the
# trademark caution are in its README). Root-anchored so those paths are not
# swallowed by this entry.
/fixtures/

# test artifacts
.coverage
