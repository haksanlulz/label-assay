.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.DS_Store

# A scraped COLA evaluation corpus is not committed (public-registry artwork
# may carry the applicant's own copyright, and it is large). The synthetic test
# corpus under tests/fixtures/labels/ IS committed — root-anchored so that path
# is not swallowed by this entry. Regenerate: uv run python tools/make_test_labels.py
/fixtures/
